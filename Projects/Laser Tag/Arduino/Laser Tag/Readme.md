Demo of leds with classes, copied from https://paulmurraycbr.github.io/ArduinoTheOOWay.html

This file is divided into multiple .cpp classes.

Use `sh ./build.sh` to concatenate all files into one on `./build` folder.

